# Sponsor Findings for "Typesense"

**Date:** 2025-11-22

**Investigation:**
Checked for the sponsor "Typesense" in the `rubycon.it/_data/sponsors.yml` file on both `main` and `staging` branches, and reviewed the git history of the file on the `main` branch.

**Findings:**

1.  **"Typesense" Sponsor Status:**
    *   The sponsor "Typesense" is **not present** in `rubycon.it/_data/sponsors.yml` on the `main` branch.
    *   The sponsor "Typesense" is **not present** in `rubycon.it/_data/sponsors.yml` on the `staging` branch.
    *   The git history of `rubycon.it/_data/sponsors.yml` on the `main` branch does not indicate any past inclusion or removal of "Typesense".

2.  **Branch Synchronization (Sponsors):**
    *   The `staging` branch is **behind** the `main` branch regarding sponsor information.
    *   Specifically, `main` branch includes "Helvetic Ruby" and "RubyConf Austria" as community sponsors, which are not present on the `staging` branch.

**Conclusion:**
"Typesense" has not been added as a sponsor to either the `main` or `staging` branches of the `rubycon.it` repository.
