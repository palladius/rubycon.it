DSL is now ready and deployed! 🚀

- **Format:** Single-line Ruby DSL (concise & readable)
- **Colors:** Ruby Red theme for strings, Cyan for keywords.
- **Components:** Split into `definition.rb` and `content.rb` (which is included in the page).
- **Features:** Supports `talk`, `pause`, `check_in`, `lightning_talks`, `party` with optional args (tags, speakers).

Check it out at `/program_of_the_day` (unlinked page).

Screenshot:
(See artifact)
